checks:
  aws:
    storageHostname: "" # CHANGE ME: set Cloudflare custom hostname for coprocessor S3 bucket
  db:
    rdsSecretArn: "" # CHANGE ME: ARN of the RDS master credentials secret in AWS Secrets Manager
