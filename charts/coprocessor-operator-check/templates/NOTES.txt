Coprocessor Operator infra-config check executed. Job logs available for {{ .Values.job.ttlSecondsAfterFinished }}s:

  kubectl logs -n {{ .Release.Namespace }} -l app=coprocessor-operator-check --all-containers --tail=-1
